## 1️⃣ Quản lý tập tin trong Linux là gì?
Quản lý tập tin trong Linux là cách hệ điều hành:
* Tổ chức dữ liệu thành **file (tập tin)**
* Sắp xếp file vào **thư mục (directory)**
* Lưu trữ, truy xuất, chỉnh sửa và bảo vệ dữ liệu

Trong Linux, mọi thứ đều là file:
* Chương trình → file nhị phân
* Mã nguồn → file văn bản
* Ổ cứng, thiết bị mạng → cũng được biểu diễn dưới dạng file
* File do người dùng tạo: `.txt`, `.doc`, `.pdf`, `.xlsx`, v.v.

Tất cả file được tổ chức theo cấu trúc cây (filesystem tree).

## 2️⃣ Hệ thống tập tin (Filesystem)
Filesystem có nhiệm vụ:
* Lưu trữ dữ liệu trên ổ đĩa
* Quản lý cách truy xuất và cập nhật dữ liệu

Ví dụ các filesystem phổ biến:
* FAT16, FAT32
* NTFS
* Ext2, Ext3, Ext4 (Linux dùng phổ biến)

## 3️⃣ Các loại tập tin trong Linux
Có 3 loại chính:
### 🔹 1. Tệp thông thường (Regular file)
* Chứa dữ liệu: văn bản, chương trình, ảnh, nhạc…
* Ký hiệu: `-`

### 🔹 2. Thư mục (Directory)
* Chứa các file khác
* Ký hiệu: `d`

### 🔹 3. Tập tin đặc biệt (Special file)
* Đại diện cho thiết bị phần cứng
* Hoặc cơ chế giao tiếp giữa tiến trình

Một số ký hiệu thường gặp:

| Ký hiệu | Ý nghĩa                      |
| ------- | ---------------------------- |
| -       | File thường                  |
| d       | Thư mục                      |
| l       | Liên kết mềm (symbolic link) |
| b       | Block device                 |
| c       | Character device             |
| p       | Named pipe                   |
| s       | Socket                       |


## 4️⃣ Các lệnh quản lý tập tin quan trọng
### 📌 Liệt kê file
ls

Xem chi tiết:
ls -l

Hiển thị cả file ẩn:
ls -a

## 5️⃣ File ẩn trong Linux
* Tên bắt đầu bằng dấu `.`
  Ví dụ:
* `.profile`
* `.bashrc`
* `.kshrc`

Ký hiệu đặc biệt:
* `.` → thư mục hiện tại
* `..` → thư mục cha

## 6️⃣ Tạo và chỉnh sửa file
### Tạo file bằng vi
vi filename

Vào chế độ sửa:
Nhấn `i`

Thoát và lưu:
Esc
Shift + ZZ

## 7️⃣ Xem nội dung file
cat filename

Đánh số dòng:
cat -b filename

## 8️⃣ Đếm số dòng, từ, byte
wc filename

Kết quả hiển thị:
* Số dòng
* Số từ
* Số byte
* Tên file

## 9️⃣ Sao chép file
cp source_file destination_file

## 🔟 Đổi tên hoặc di chuyển file
mv old_name new_name

## 1️⃣1️⃣ Xóa file
rm filename

An toàn hơn:
rm -i filename

## 1️⃣2️⃣ Tạo liên kết (Link)

### Liên kết mềm (Symbolic link)
ln -s filename symlink

### Liên kết cứng (Hard link)
ln filename hardlink

Khác biệt:
* Soft link: giống shortcut, xóa file gốc → link hỏng
* Hard link: cùng inode, xóa 1 file → dữ liệu vẫn còn nếu còn link khác




